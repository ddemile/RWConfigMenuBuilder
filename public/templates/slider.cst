ColorUtility.TryParseHtmlString("$"COLOR"", out color);
Tabs[$"PAGEINDEX"].AddItems(
    new OpSlider($"CONFIGURABLE", new Vector2($"X"f, $"Y"f), $"WIDTH")
    {
        colorEdge = color,
        colorLine = color,
        description = $"CONFIGURABLE".info.description
    }
);